import Content from './content.mdx'

export const metadata = {
	title: 'Spans',
	description:
		'Deep dive into Vestig Span API. Create spans, track operations, measure duration, and build trace hierarchies.',
}

export default function Page() {
	return <Content />
}
