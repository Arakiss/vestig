import Content from './content.mdx'

export const metadata = {
	title: 'OTLP Export',
	description:
		'Export traces to any OpenTelemetry-compatible backend using the OTLP protocol. Zero dependencies, native fetch, Edge-compatible.',
}

export default function Page() {
	return <Content />
}
