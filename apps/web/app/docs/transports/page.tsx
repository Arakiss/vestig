import Content from './content.mdx'

export const metadata = {
	title: 'Transports',
	description:
		'Configure Vestig log destinations. Console, HTTP, file transports. Send logs to Datadog, Sentry, or custom endpoints.',
}

export default function Page() {
	return <Content />
}
