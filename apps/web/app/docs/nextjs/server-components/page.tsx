import Content from './content.mdx'

export const metadata = {
	title: 'Server Components',
	description:
		'Logging in React Server Components with Vestig. Automatic context propagation and request correlation.',
}

export default function Page() {
	return <Content />
}
