import Content from './content.mdx'

export const metadata = {
	title: 'Client Components',
	description:
		'React hooks and providers for client-side logging with Vestig. useLogger hook and VestigProvider.',
}

export default function Page() {
	return <Content />
}
