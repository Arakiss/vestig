import Content from './content.mdx'

export const metadata = {
	title: 'Tail Sampling',
	description:
		'Keep 100% of errors and slow requests while sampling successful operations to reduce costs.',
}

export default function Page() {
	return <Content />
}
