import Content from './content.mdx'

export const metadata = {
	title: 'PII Sanitization',
	description:
		'Protect sensitive data in logs with Vestig. GDPR, HIPAA, PCI-DSS presets. Automatic redaction of emails, credit cards, SSNs.',
}

export default function Page() {
	return <Content />
}
